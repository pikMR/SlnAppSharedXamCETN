<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AppCETN.ViewModels"
             x:Class="AppSharedXamCETN.MainPage">
    <ScrollView>
        <StackLayout>
            <Button x:Name="PressAdd"
        Text="{Binding Add}"
        Clicked="PressAdd_Clicked"/>
            <local:LestaInicioView ItemsSource="{Binding Items}" ItemClickCommand="{Binding ItemClickCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
